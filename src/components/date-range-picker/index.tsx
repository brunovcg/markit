import { DateRangePicker as DTP } from "react-date-range-ts";
import { useState } from "react";

function DateRangePicker() {
  // const [startDate, setStartDay] = useState(new Date());
  // const [endDate, setStartDay] = useState(new Date());

  // const selectionRange = {
  //   startDate,
  //   endDate,
  //   key: "selection",
  // };

  // const handleSelect = () => {};

  return (
    <div>
      {/* <DTP ranges={[selectionRange]} onChange={handleSelect} /> */}
    </div>
  );
}

export default DateRangePicker;
