import styled from "styled-components";

export const StyledTimeEntries = styled.section`
  display: flex;
  align-items: center;
  justify-content: center;

  .time{
    font-size: 25px;
    margin:30px 20px 30px 0;
    color: var(--dark-font);
    font-family: var(--text-font);
  }
`;
